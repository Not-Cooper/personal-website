---
import { getCollection, render, getEntry, type RenderResult } from 'astro:content';

const { index, posts } = Astro.props;

const renderedPosts: RenderResult[] = [];

for (const post of posts) {
    const Content = await render(post)
    renderedPosts.push(Content)
}

const { Content } = renderedPosts[index]
---

<Content/>